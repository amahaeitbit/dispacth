# Required for the Agents SDK to call OpenAI models.
OPENAI_API_KEY=sk-...

# Model used by all agents. gpt-4o-mini keeps the demo cheap; bump for quality.
ORDER_AGENT_MODEL=gpt-4o-mini

# Where the SQLite file lives (relative paths are resolved from backend/).
DISPATCH_DB_PATH=dispatch.db
